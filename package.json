{
  "devDependencies": {
    "@types/chrome": "^0.1.37"
  },
  "scripts": {
    "test": "node -e \"require('child_process').exec(`chrome \"file:///${require('path').join(process.cwd(),'test','test.html')}\"`)\";",
    "dist": "7z a dist.zip src icons manifest.json"
  }
}
